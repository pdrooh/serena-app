# ============================================
# VARIÁVEIS PARA RAILWAY - BACKEND
# ============================================
# 
# INSTRUÇÕES:
# 1. No Railway, vá no serviço do BACKEND
# 2. Clique em "Variables"
# 3. Adicione cada variável abaixo
# 4. Para JWT_SECRET, clique em "Generate" no Railway
# 5. Para CORS_ORIGIN, use a URL do seu frontend
#
# ============================================

# Ambiente
NODE_ENV=production

# Porta do servidor
PORT=5001

# Cliente do banco de dados
DB_CLIENT=postgresql

# JWT Secret (GERE UMA CHAVE SEGURA - use "Generate" no Railway)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS - URL do frontend (substitua pela URL real do seu frontend)
CORS_ORIGIN=https://seu-frontend.railway.app

# SSL para PostgreSQL (geralmente necessário no Railway)
DB_SSL=true

# Rate Limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# ============================================
# VARIÁVEIS DO POSTGRESQL (AUTOMÁTICAS)
# ============================================
# 
# IMPORTANTE: Estas variáveis são criadas AUTOMATICAMENTE
# quando você conecta o PostgreSQL ao backend no Railway.
# 
# NÃO adicione essas manualmente! Use "Add Reference" no Railway.
#
# PGHOST=(criado automaticamente)
# PGPORT=(criado automaticamente)
# PGDATABASE=(criado automaticamente)
# PGUSER=(criado automaticamente)
# PGPASSWORD=(criado automaticamente)
#
# ============================================

